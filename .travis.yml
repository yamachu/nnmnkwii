language: python

python:
  - "2.7"
  - "3.6"

notifications:
  email: false

before_install:
  - sudo apt-get update
  - if [["$TRAVIS_PYTHON_VERSION" == "2.7"]]; then
      wget http://repo.continuum.io/miniconda/Miniconda-3.8.3-Linux-x86_64.sh -O miniconda.sh;
    else
      wget http://repo.continuum.io/miniconda/Miniconda3-3.8.3-Linux-x86_64.sh -O miniconda.sh;
    fi
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  # Useful for debugging any issues with conda
  - conda config --add channels pypi
  - conda info -a
  - deps='pip numpy=1.15.4=py36h1d66e8a_0	scipy=1.1.0=py36hfa4b5c9_1 cython nose pytorch mkl=2018.0.3=1 mkl_fft=1.0.6=py36h7dd41cf_0 mkl_random=1.0.1=py36h4414c95_1'
  - conda create -q -n test-environment "python=$TRAVIS_PYTHON_VERSION" $deps -c pytorch
  - source activate test-environment
  - pip install codecov flake8

install:
  - pip install -e ".[test]"

script:
  - nosetests --with-coverage --cover-package=nnmnkwii -v -w tests/ -a '!require_local_data,!modspec'
  - flake8

after_success:
  - codecov
